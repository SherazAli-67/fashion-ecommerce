import 'package:flutter/cupertino.dart';

class MessagingScreen extends StatelessWidget{
  const MessagingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Messaging screen"),);
  }
}